syntax = "proto3";

package tari.p2pool.sharechain.rpc;

import "block.proto";

service ShareChain {
  // Sync will return a stream of share chain blocks starting from `from_height`.
  rpc Sync(SyncRequest) returns(stream Block);
}

message SyncRequest {
  uint64 from_height = 1;
}